# Réservé pour une future étape de synthèse IA (non utilisée par run_collect.py) —
# voir APPEL_OFFRES/.env.example pour le détail.
GEMINI_API_KEY=
ANTHROPIC_API_KEY=

# Si un compte "opérateur économique" SIGOMAP devient disponible (Côte d'Ivoire),
# renseigner ici — jamais en dur dans le code.
SIGOMAP_EMAIL=
SIGOMAP_PASSWORD=
